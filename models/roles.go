// roles.go kept for backward-compatibility. Role constants are defined in
// errors_model.go under the Status Constants section (e.g. RoleSuperAdmin,
// RoleAdmin, RoleFaculty, RoleStudent). Use those constants instead.

package models
